// Example array
const numbers = [3, 7, 2, 9, 5];


let sum = 0;

// Loop through the array starting from the second element
for (let i = 1; i < numbers.length; i++) {
    sum += numbers[i]; // Update sum by adding current element
    
    
}

console.log("Sum is:", sum);